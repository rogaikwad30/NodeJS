exports.getItems = (req, res) => {
  // TODO: get list of items from database and return as response
};

exports.createItem = (req, res) => {
  const { name, price } = req.body;
  // TODO: create new item in database and return success response
};
